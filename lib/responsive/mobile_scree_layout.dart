import 'package:flutter/material.dart';

class MobileScreeLayout extends StatelessWidget {
  const MobileScreeLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Thi is mobile'),);
  }
}